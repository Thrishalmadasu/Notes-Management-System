apiVersion: v1
kind: Service
metadata:
  name: notes-api-service
  labels:
    app: notes-api
spec:
  type: NodePort
  selector:
    app: notes-api
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  sessionAffinity: None
